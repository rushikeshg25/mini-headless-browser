{
  "name": "mini-headless-browser",
  "version": "0.2.0",
  "description": "A tiny headless browser engine built from scratch in TypeScript (no Chromium).",
  "type": "module",
  "bin": {
    "mhb": "./browser.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --test \"test/**/*.test.ts\""
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  },
  "license": "MIT"
}
